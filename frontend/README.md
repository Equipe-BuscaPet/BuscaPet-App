# Frontend

Consome o contrato OpenAPI publicado pelo backend (`api/`, gerado
automaticamente pelo FastAPI em `/docs`). Protótipo das telas principais em
`docs/sprint2/05-prototipos.md`.

Framework e estrutura de pastas a definir por quem assumir o frontend na
equipe — nada disso está travado ainda, só o contrato de API (Sprint 1) e os
wireframes (Sprint 2).
